describe("The Reg Number factory function", function() {
	
	it("should add unique reg numbers", function() {
		assert.equal(1,2);
	});

	it("should filter for a town", function() {
		assert.equal(1,2);
	});

	it("should give error error message for an invalid regNumber", function() {
		assert.equal(1,2);
	});



});